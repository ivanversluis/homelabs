apiVersion: v1
kind: ConfigMap
metadata:
  name: fm-config
  # namespace set by overlay
  labels:
    app: firewall-manager
data:
  FM_ENV: "development"  # Options: dev, staging, prod
  GIT_COMMIT_SHA: ""     # Updated by CI/CD pipeline
  GIT_REPO_URL: "https://github.com/ivanversluis/firewall-manager"
