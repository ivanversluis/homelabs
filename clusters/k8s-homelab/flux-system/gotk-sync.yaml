# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/ivanversluis/homelabs
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/k8s-homelab
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # DO NOT add postBuild.substituteFrom here.
  # This root Kustomization renders ./clusters/k8s-homelab which includes
  # flux-system/gotk-components.yaml — the Flux CRD definitions themselves.
  # Those CRDs contain documentation strings matching ${var:=default} patterns
  # that envsubst treats as substitution targets, causing reconciliation failures.
  #
  # Variable substitution is intentionally scoped to dedicated per-component
  # Kustomization objects that target specific paths:
  #   infra/cert-manager-config, infra/kong, infra/ai, infra/observability,
  #   infra/coredns, infra/identity-ingress, apps/homepage, apps/homebox,
  #   apps/forgejo, apps/vaultwarden
  # Each of those declares its own postBuild.substituteFrom: flux-domain-vars.
