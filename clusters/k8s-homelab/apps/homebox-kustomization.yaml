---
# Flux Kustomization for Homebox inventory management.
# Depends on: kong (IngressClass + wildcard cert must be ready) and flux-domain-vars Secret.
# The postBuild substituteFrom replaces ${DOMAIN} in manifests.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: homebox
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/homebox
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: kong
  postBuild:
    substituteFrom:
      - kind: Secret
        name: flux-domain-vars
