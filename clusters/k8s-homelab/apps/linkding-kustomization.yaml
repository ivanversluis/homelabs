---
# Flux Kustomization for Linkding (bookmarks).
# Separate from the main apps path because linkding-deployment.yaml uses ${DOMAIN}
# for OIDC issuer endpoints and the bookmarks.${DOMAIN} public URL.
# Depends on: kong (ingress + OIDC callback routes through Kong).
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: linkding
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/linkding
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: kong
  postBuild:
    substituteFrom:
      - kind: Secret
        name: flux-domain-vars
