apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Runtime, shared policy, TLS and Helm resources.
  - core
  # General API hostname and gateway health route.
  - api-gateway
  # LLM routes and their authentication/translation plugins.
  - llm-gateway
