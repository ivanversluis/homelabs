apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager-namespace.yaml
  - cert-manager-helmrepository.yaml
  - cert-manager-helmrelease.yaml
  # ClusterIssuers and the Cloudflare token ExternalSecret are in
  # infra/cert-manager-config/ which has its own Flux Kustomization
  # that dependsOn this one + healthChecks on the HelmRelease.
