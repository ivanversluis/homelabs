apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - mcp-rbac.yaml
  - mcp-externalsecret.yaml
  - mcp-inject-api-key-config-externalsecret.yaml
  - mcp-deployment.yaml
  - mcp-service.yaml
  # KongPlugin and KongConsumer resources — Secret (openwebui-acl-group) must
  # come before KongConsumer so the Kong admission webhook can validate credentials.
  - kong-plugin-mcp-key-auth.yaml
  - kong-plugin-mcp-bearer-to-apikey.yaml
  - kong-plugin-mcp-oidc.yaml
  - mcp-ingress.yaml
