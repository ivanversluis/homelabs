apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcpo
  namespace: ai
  labels:
    app.kubernetes.io/name: kubernetes-mcpo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubernetes-mcpo
  ports:
    - name: http
      port: 8000
      targetPort: 8000
