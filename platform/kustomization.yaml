apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Platform resources that are intentionally reconciled by the root Flux path.
  # Components with dedicated Flux Kustomizations are not listed here.
  - security/external-secrets/
  - networking/network-policies/

# local-path-provisioner has its own dedicated Flux Kustomization
# (clusters/k8s-homelab/platform/local-path-provisioner-kustomization.yaml),
# like longhorn — not listed here. See platform/storage/README.md.
